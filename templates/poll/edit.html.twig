{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="h3 mb-3 font-weight-normal">Editer un sondage</h1>

    {{ include('poll/_form.html.twig') }}

    <a href="{{ path('poll_index') }}">back to list</a>

    {{ include('poll/_delete_form.html.twig') }}
{% endblock %}
